a. SELECT FIRST_NAME AS FIRST NAME
   FROM employee;
b.
c.SELECT * FROM EMPLOYEE ORDER BY FIRST_NAME ASC
d.SELECT * FROM EMPLOYEE WHERE FIRST_NAME LIKE '%o%';
e.
f.SELECT DEPARTMENT,SUM(SALARY) TOTAL_SALARY FROM EMPLOYEE GROUP BY DEPARTMENT
ORDER BY TOTAL_SALARY DESC;
g.SELECT DEPARTMENT,MAX(SALARY) MAXSALARY
FROM EMPLOYEE GROUP BY DEPARTMENT ORDER BY MAXSALARY ASC;
h.SELECT First_Name,Incentive_AMT 
FROM employee A INNER JOIN incentives B 
  ON A.EM_ID=B.Incentive_Id
AND Incentive_AMT>3000;
i.SELECT * FROM employee where max(salary) from employee where salary
(SELECT max(salary)from employee);
j.
k.
l.
m.
